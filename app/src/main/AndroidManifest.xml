<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.sakuqi.androidmaterialdesign">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:name=".AppApplication"
            android:theme="@style/AppTheme">
        <activity android:name=".SnapRecyclerViewActivity">
        </activity>
        <activity android:name=".behavior.CustomBehaviorActivity">
        </activity>
        <activity android:name=".ToolbarActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>

                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>

                <data
                        android:host="www.sakuqi.com"
                        android:pathPattern="/ResourceQuation_War"
                        android:scheme="https"/>
            </intent-filter>
        </activity>
    </application>

</manifest>